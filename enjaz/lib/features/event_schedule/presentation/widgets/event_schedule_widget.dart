 import 'package:flutter/material.dart';

class EventScheduleWidget extends StatelessWidget {
  const EventScheduleWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Placeholder();
  }
  }
